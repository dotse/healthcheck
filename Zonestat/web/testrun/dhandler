<h1>Test run <% $tr->name %> for <% $tr->domainset->name %></h1>

<h2>Common problems</h2>
<& /table/common_problems, sid => $tid &>

<h2>Number of tests by highest severity problem</h2>
<& /table/max_severity, sid => $tid &>

<h2>Webserver software</h2>
<img src="/chart/webserver?sid=<% $tid %>">
<& /table/webserver_types, sid => $tid &>

<h2>Webserver software (https)</h2>
<img src="/chart/webserver?https=1&sid=<% $tid %>">
<& /table/webserver_types, sid => $tid, https => 1 &>

<h2>Unknown webserver identifications</h2>
<& /table/unknown_server_types, sid => $tid &>

<h2>DNSCheck</h2>
<& /table/all_tests, sid => $tid &>

<form action="/com/delete_testrun" method="post">
    <input type="hidden" name="tid" value="<% $tid %>">
    <input type="submit" value="Delete this test run">
</form>

<%shared>
$r->content_type('text/html');
my $tid = $m->dhandler_arg;
$m->clear_buffers, $m->abort(404) unless defined($tid);
my $tr = $zs->present->dbx('Testrun')->find($tid);
$m->clear_buffers, $m->abort(404) unless defined($tr);
</%shared>
<%method title>ZoneStat: Testrun <% $tr->name %></%method>