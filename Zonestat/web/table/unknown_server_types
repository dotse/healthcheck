        <table>
            <tr>
                <th>Server Identification String</th>
            </tr>
% for my $i (@data) {
                <tr class="<% ++$c%2==0?'even':'odd' %>">
                    <td><% $i %></td>
                </tr>
% }
            <tr>
                <th colspan="8"><a href="/csv/unknown_server_types<% (defined($sid) ? "?sid=$sid" : "") %>">CSV file</a></th>
            </tr>
        </table>
        
<%init>
my $ds;
if (defined($sid)) {
    $ds = $zs->present->dbx('Testrun')->find($sid);
    $m->abort(404) unless defined($ds);
}

my @data  = $zs->present->unknown_server_strings($ds);
my $c;
</%init>
<%args>
$sid => undef
</%args>